<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="TextLabelDummy" parent="Widget.Design.TextInputLayout">
        <item name="colorAccent">@color/black</item>  // Text Color
        <item name="android:textColorHint">@color/black</item>    // Hint Color
        <item name="colorControlActivated">@color/white</item>    //Floating label color
    </style>

</resources>